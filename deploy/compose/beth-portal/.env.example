# Beth portal local binding.
BETH_PORTAL_BIND_IP=0.0.0.0
BETH_PORTAL_HOST_PORT=8091

# Freyja 5 OpenAI-compatible endpoint. The token stays server-side in the
# portal proxy and is never sent to the browser.
FREYJA_OPENAI_BASE_URL=http://host.docker.internal:8500/v1
FREYJA_API_KEY=
